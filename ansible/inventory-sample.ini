# Ansible inventory — update the IP after CDK deploy
# CDK outputs the public IP as "AnsibleTarget"
[api]
# <EC2_PUBLIC_IP> ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/<your-key>.pem ansible_python_interpreter=/usr/bin/python3.9
